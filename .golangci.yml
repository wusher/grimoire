version: "2"

linters:
  settings:
    errcheck:
      # Terminal output is best effort. There is no useful recovery after the
      # command's stdout or stderr has already failed.
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
